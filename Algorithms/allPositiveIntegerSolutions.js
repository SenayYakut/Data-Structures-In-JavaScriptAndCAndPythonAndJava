//Print all positive integer solutions to the equation a^3+b^3=c^3+d^3 where a,b,c,d are integers between 1 and 1000
// brute force solution but so many memory consumption
for (i=0;i<;10;i++)
		for (j=0;j<;10;j++)
			hashmap[x,a,b] = a^3+b^3;
	for (i=0;i<;10;i++)
		for (j=0;j<;10;j++)
			if c^3+d^3 == hashpmap[x]
				printf("a, b, c, d");